---
name: handover-docs
description: リポジトリを読み込んで、退職・異動・チーム移管のための引き継ぎ資料（ハンドオーバー資料）を作成するスキル。ユーザーが「引き継ぎ資料を作って」「退職するのでドキュメントをまとめたい」「このプロジェクトを誰かに引き継ぐ」「ハンドオーバー資料」「handover」などと依頼したときに使う。コードベースを体系的に調査し、新しい担当者がプロジェクトを引き継げるだけの情報を1つのMarkdownにまとめる。
---

# 引き継ぎ資料作成スキル (Handover Docs)

退職・異動・移管のときに、リポジトリを調査して **新しい担当者がこのプロジェクトを引き継げる** だけの情報を 1 つの Markdown にまとめる。

## このスキルのゴール

「次の人が、これ 1 枚を読めば動かせて・直せて・困ったときに辿れる」状態を作る。
コードを読めば分かることより、**コードを読んでも分からないこと**（背景・経緯・暗黙知・運用・連絡先）を優先して残す。

## 進め方

### 1. リポジトリを体系的に調査する

以下を順に確認し、メモを取りながら進める。`Read` / `Glob` / `Grep` と、必要なら `Explore` エージェントを使う。

- **概要**: `README*`, `docs/`, `CONTRIBUTING*`, `ARCHITECTURE*`, Wiki へのリンク
- **技術スタック・依存**: `package.json`, `requirements.txt`, `pyproject.toml`, `go.mod`, `Gemfile`, `pom.xml`, `Cargo.toml` など
- **構成**: トップレベルのディレクトリ構造と、各ディレクトリの役割
- **エントリポイント**: `main.*`, `index.*`, `app.*`, `cmd/`, ルーティング定義
- **ビルド/実行/テスト**: `Makefile`, `package.json` scripts, `Taskfile`, `tox.ini`, `docker-compose*`, `Dockerfile`
- **CI/CD**: `.github/workflows/`, `.gitlab-ci.yml`, `.circleci/`, `Jenkinsfile`, デプロイ設定
- **インフラ/環境**: `terraform/`, `k8s/`, `helm/`, `serverless.yml`, `*.tf`, IaC, 環境変数の定義（`.env.example` 等）
- **外部サービス連携**: SDK・API クライアントの利用箇所、Webhook、cron/スケジューラ
- **設定・シークレットの「在りか」**: `.env.example`, secrets manager 参照、`config/` （**値そのものは資料に書かない**）
- **既知の課題**: コード中の `TODO` / `FIXME` / `HACK` / `XXX`、`Issues`、未マージの重要 PR
- **経緯**: `git log` の主要コミット、リリースタグ、`CHANGELOG*`

調査時の注意:
- シークレットや認証情報の**値**を資料に転記しない。「どこに保管されているか（例: AWS Secrets Manager の `prod/db`）」だけを書く。
- 推測で断定しない。確認できなかった項目は空欄にせず「**要確認**」と明示する。

### 2. テンプレートを埋める

`TEMPLATE.md` を雛形として使い、調査結果を流し込む。記入例は `examples/SAMPLE_HANDOVER.md` を参照。

- コードから機械的に分かること（スタック、コマンド、構成）は埋める。
- 人にしか分からないこと（連絡先、関係者、社内ツールの権限、業務背景、運用の暗黙知）は、ユーザーに質問するか、`> 🖊 要記入:` のプレースホルダを残してユーザーが後で埋められるようにする。
- 各セクションで「確認できた事実」と「推測・要確認」を区別する。

### 3. ユーザーに不足情報を確認する

コードからは絶対に分からない以下は、`AskUserQuestion` でまとめて聞くか、ドラフトに `要記入` として残す:
- 関係者・連絡先（後任、PO、SRE、ベンダー）
- 進行中タスク・未着手の TODO・ロードマップ
- 運用上の暗黙知（「毎月末にこのバッチを手動で叩く」等）

### 4. 成果物を書き出す

`HANDOVER.md` としてリポジトリ直下（または `docs/`）に出力する。出力後、**要確認 / 要記入** が残っている箇所を一覧でユーザーに報告する。

## 必要な項目（資料の構成）

以下の 3 つの大セクションに分けて構成する。詳細な雛形は `TEMPLATE.md` を参照。

**① 案件概要** — このプロジェクトが何で、誰が関わっているか
- プロジェクト概要（何のためのもの・ビジネス上の役割）
- 技術スタック / アーキテクチャ
- 関係者・連絡先
- 関連ドキュメント・リンク集

**② 開発環境セットアップ** — 手元で動かして変更を出すまで
- リポジトリ構成
- ローカル開発環境のセットアップ手順
- ビルド・テスト・デプロイ手順（CI/CD含む）
- 設定とシークレットの管理場所

**③ 運用・保守** — 本番を動かし続けるために
- 実行環境・インフラ（本番/ステージング、ホスティング先）
- 外部サービス・依存・連携先
- 定期作業・運用タスク（cron, バッチ, 監視）
- 既知の課題・技術的負債・落とし穴
- よくあるトラブルと対応（ランブック）
- 進行中の作業・TODO・今後の予定
